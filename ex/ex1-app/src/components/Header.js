import React from 'react';

const Header = ( {number}) => {
    return (
        <div className='header'>
            Header 영역입니다 : {number}
        </div>
    );
};

export default Header;